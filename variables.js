const token = 'token'; // token ID determined when bot is created
const clientId = 'client'; // application ID found in discord developer settings
const guildId = 'guild'; // primary server ID

const devID = 'dev'; // developer's ID. may need to change to array later?
const prefix = 'g'; // prefix used for chat commands

module.exports = {
	token, 
	clientId,
	guildId,
	devID,
	prefix
}